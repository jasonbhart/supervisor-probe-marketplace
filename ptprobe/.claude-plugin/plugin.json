{
  "name": "ptprobe",
  "version": "0.1.3",
  "description": "exp-08 diagnostic: Prooftrail's real client code under a non-colliding name, carrying only the doctor skill. Run /ptprobe:doctor --verbose in a Cowork sandbox to report that surface's plugin layout, hook delivery and environment. Log-only; submits nothing unless SERVICE_URL is set.",
  "author": {
    "name": "Jason (Domain Methods)"
  },
  "userConfig": {
    "SERVICE_URL": {
      "type": "string",
      "title": "Review service URL",
      "description": "Origin of the review API. Defaults to the hosted service; override for self-hosted deployments.",
      "default": "https://supervisor-api.jason-8e5.workers.dev"
    },
    "API_TOKEN": {
      "type": "string",
      "title": "Device token",
      "description": "Paired device token. Leave blank on the host (run /prooftrail:setup instead); set this only for sandbox surfaces, whose plugin data is wiped per session.",
      "required": false,
      "sensitive": true
    }
  }
}
